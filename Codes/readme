You can find the example input data in the "examples of processed data" for the train and test codes.

BMES_BERT_output.csv and WBD_BERT_output.csv are examples output results of train on test.

And they are also example input data for the evaluation codes.
